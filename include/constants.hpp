#ifndef CONSTANTS_HPP
#define CONSTANTS_HPP

namespace Constants {
    const float FIELD_HALF_WIDTH = 0.98f;
    const float FIELD_HALF_HEIGHT = 0.78f;
    const float FIELD_BOUNDARY_X = 0.93f; // Linha branca
    const float FIELD_BOUNDARY_Y = 0.73f; // Linha branca
    const float GOAL_HALF_WIDTH = 0.18f;
    const float GOAL_DEPTH = 0.05f;
    const float PENALTY_AREA_WIDTH = 0.22f;
    const float PENALTY_AREA_HEIGHT = 0.32f;
}

#endif
